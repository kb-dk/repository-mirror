# RUN

# Repository Pull
I use this directory for running the GitPull daemon, setting the directory to be owned by tomcat

# Database push
I use this directory for running the GitPull daemon, setting the directory to be owned by tomcat
